.block
  .title
    Recent Comments
  .body
    - Comment.all(:limit => 10, :order => [:created_at.desc], :created_at.gt => 1.week.ago).each do |comment|
      .row
        .comment= link comment.content.truncate(20), :to => (resource(comment.post) + "#comment_#{comment.id}")
        .author= comment.author.truncate(18)
        .date= relative_date(comment.created_at).capitalize
        